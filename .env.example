# Gemini API key used by the app (inlined at build time by Vite).
# Locally: copy this file to .env.local and set your key.
# On Vercel: add GEMINI_API_KEY as an Environment Variable in Project Settings.
GEMINI_API_KEY=your_gemini_api_key_here
